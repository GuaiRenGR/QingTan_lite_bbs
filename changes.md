# 更新日志

## v1.2.5

### 聊天记录 BBCode

- 新增 `[chatlog=id]` 聊天记录 BBCode，聊天记录 JSON 文件通过 OneDrive 保存。
- 支持文字消息、图片消息、引用消息和聊天记录嵌套，单条记录最多 100 条消息，最多嵌套两层。
- 新增聊天记录编辑器，可在发帖和编辑帖子时创建、修改聊天记录。
- 聊天记录以 QQ 风格摘要卡片显示，点击后进入独立聊天记录页，以聊天软件界面展示完整消息。
- 聊天记录中的图片不会作为帖子封面，帖子摘要也不会显示聊天记录标签。
- 服务端增加 JSON 格式、消息类型、数量和嵌套层级校验。
- 升级已有服务端时需覆盖新版 `server/` 文件并执行 `server/upgrade_version_1_2_5.php`。

### 版本

- 版本更新为 `1.2.5+31`

## v1.2.4

### 音乐播放器

- 播放器新增参考 NeriPlayer HyperBackground 的跨平台动态背景
- 从歌曲封面量化提取 Base、Accent、Light、Dark、Bridge 五组颜色，并针对深浅色模式调整饱和度与明度
- 动态色场改用 GPU Fragment Shader 单次绘制，避免逐帧创建多组径向渐变
- 设置新增“正在播放模糊封面背景”，默认模糊强度 40、背景调暗 0.4，并默认开启
- 模糊封面背景与动态背景、音频律动互斥；动态背景和音频律动默认关闭
- 播放器前景固定使用暗色页面配色，修复动态取色背景下歌词难以辨认的问题
- 播放、进度和翻页等播放器控件跟随当前音乐封面取色，并修复歌曲名、时间和进度条显示过暗的问题
- 播放进度更新不再重建整个播放器页

### 主题与 Android

- “我的”页右上角新增主题色入口，提供轻坛粉、MD3 经典蓝等六种颜色并持久保存
- 首页标签、底部导航、刷新指示器等核心控件跟随主题色
- 修复 Android 启动及运行时系统导航栏显示黑色的问题，导航栏现在与应用背景一致
- 设置页等待本地偏好加载完成后再显示，避免先闪现默认设置
- 删除“自动播放帖子中的音乐”和“自动播放视频”两个设置项

### X 信息流与浏览体验

- 已点赞状态兼容布尔、数字及旧服务端字符串格式
- “显示更多”改为装饰文本，点击文本区域直接进入帖子详情
- 点击观看次数以 Bottom Sheet 展示说明和“忽略”按钮
- 昵称、用户名自适应收缩并优先完整显示发帖时间
- 评论右上角更多按钮改为浅灰色
- 浏览历史显示最后查看时间，并在客户端按帖子去重

### 版本

- 版本更新为 `1.2.4+30`

## v1.2.3

### X 信息流

- 用户昵称右侧改为显示用户名，不再使用数字用户 ID
- 用户头像改为方形小圆角样式，右下角发帖按钮固定为圆形
- 帖子标题或摘要显示不全时，在末尾显示蓝色“显示更多”并支持展开
- 回复、转发、点赞和分享按钮接入实际功能，浏览量继续作为只读统计显示
- 发帖时间按“刚刚”“X分钟”“X小时”“X天”或完整日期时间显示，所有相对时间不带“前”字

### 音乐播放

- 网络音乐使用持久化流缓存，已播放内容不会在播放过程中被自动丢弃
- 缓存进度按当前歌曲已缓存的最远位置显示，向前拖动时不再随播放进度回退
- 修复拖动播放进度后歌词可能无法继续自动滚动的问题

### 品牌与版本

- Android、iOS、Windows、macOS、Linux、Web 及应用内名称统一为“轻坛”
- 版本更新为 `1.2.3+29`

## v1.2.2

### 下载与更新

- 修复下载记录无法持久保存，应用重启后会恢复下载历史与任务状态
- Android 下载完成后发布到公共 `Download/QingTan` 目录
- Android 文件支持通过系统“使用其他应用打开”对话框选择应用
- 修复 Android 无法打开下载目录，并支持从下载管理跳转公共下载文件夹
- 增加 APK 安装权限，打开安装包时自动跳转未知来源安装授权，授权后继续安装
- 修复更新弹窗点击“立即更新”后不显示下载提示

### 信息流与显示

- 新增 X 风格纵向首页信息流，目前仅管理员可见入口
- 设置新增“缩小无图帖子占用位置”，开启后瀑布流不再显示无图占位图

## v1.2.1

- 恢复文件选择插件，修复歌词上传
- 图片帖子支持敏感内容标签、警告与屏蔽
- 默认使用 HTTPS 连接
- 音乐进度条显示预加载进度

## v1.2.0

### 音乐与工具

- “我的歌单”新增“全部播放”，可替换当前播放队列并从第一首开始播放
- 工具页新增网易云音乐搜索，搜索结果可直接使用软件音乐播放器播放并加载封面、歌词

### 多服务器

- 客户端支持从服务端发现并缓存服务器清单，缓存优先、内置常量兜底
- 设置页新增服务器测速和手动切换，界面仅展示服务器名称、状态与延迟
- 手动选择的服务器会作为请求首选，连接失败时继续自动切换可用服务器

### Bug 修复与优化

- 修复缩略图生成失败后，二次编辑帖子会误删原始图片的问题
- Android 文件选择改用 Storage Access Framework，提升第三方文件管理器兼容性
- 完善 Linux CI 的 JNI 与桌面依赖诊断

---

## v1.1.10

### 音乐库与播放

- 音乐上传后生成独立 UUID，帖子改为引用音乐库记录，同一首歌可被多个帖子复用
- 编辑器支持上传新音乐或搜索并插入已有音乐
- 新增音乐管理器，支持按歌曲名、歌手搜索已上传音乐；主搜索同步展示歌曲结果
- 客户端读取并上传音乐文件的歌名、歌手与内嵌封面信息，播放器和歌单以歌名、歌手分行显示
- 工具页新增音乐管理器入口

### Bug 修复与优化

- 网络中断时自动进入离线模式，已访问页面和“我的”页用户资料可从本地缓存读取
- 修复网络卡顿或脱机时启动校验误清除登录令牌、自动退出账号
- 修复瀑布流无图片帖子占位背景在深色模式下仍显示浅色
- 修复帖子编辑器输入框不适配深色模式，深色下使用深色背景和粉色边界
- 收紧全局控件、导航栏、瀑布流和“我的”页间距，同时保留必要触控留白
- 默认歌单按音乐 UUID 去重，修复同一首歌可重复收藏或重复插入歌单的问题
- 修复 UUID 格式的 `[music]` 标签被当作普通文本、无法渲染音乐卡片
- 播放器和音乐卡片支持服务端保存的封面地址
- 修复歌词页当前句滚动位置偏下，改为按实际歌词行居中
- 新增 `upgrade_version_1_1_10.php`，迁移旧音乐 URL 引用和旧歌单数据至 UUID 音乐库

---

## v1.1.9

### Bug 修复与优化

- 修复部分帖子短 DV 码导致插入卡片显示不可用
- DV 码改为完整 8 位空间置换，避免大量帖子前缀相同
- 升级时重新生成全部 DV，并通过别名表兼容历史链接
- 帖子卡片加载失败时自动重试，并支持点击重新加载
- 音乐卡片元数据写入磁盘缓存，相同歌曲不再重复请求附件信息和解析封面
- 播放器自动预下载下一曲，并优先从本地音乐缓存播放
- Music BBCode 支持 `[music=音乐直链,歌词直链]`，第二个 LRC 参数可省略
- 音乐上传改为音乐与可选 LRC 歌词分开选择的对话框
- 播放器支持封面/歌词左右滑动，播放列表移入左下角弹层按钮
- 播放器底部调整为播放列表、上一曲、播放/暂停、下一曲、收藏五键布局
- 工具页新增默认歌单，用户收藏歌曲会同步保存到服务端

---

## v1.1.8

### 性能与体验

- 统一图片磁盘缓存并优化缩略图内存解码，减少重复流量
- 瀑布流和图集采用小批量顺序预加载，主导航页面改为按需创建
- 修复 `hyjzbbs://thread/ID` 深度链接跳转到不存在页面
- “我的”页面改为无边框设计，发帖输入框改为白色无边框

### 新功能与修复

- 管理员新增附件文件管理、文件夹归类、移动和删除功能
- 完成 `[thread=DV码]` 插入帖子卡片渲染和帖子选择器
- 修复用户空间帖子预览泄露 `[hide]` 隐藏内容
- 首页头像改为进入“我的”页面

### 项目维护

- 删除旧核心代码快照、Claude/VS Code 本地配置和临时调试脚本
- 删除与客户端 assets 完全重复的根目录表情资源和图标副本
- 清理旧构建包、下载页面快照及其他本地临时文件
- 完善忽略规则，防止本地产物再次进入仓库

---

## v1.1.7

### 功能调整

- 客户端发帖编辑器不再把 `[img]` 代码直接渲染为图片
- 音乐播放列表保存到本地，退出或重启客户端后仍会保留
- 用户管理中的权限管理与铭牌设置拆分为独立入口

### Bug 修复

- 修复上传图片返回相对地址导致帖子图片无法显示
- 修复回复、点赞等通知因已读值类型和标记时序异常而全部显示为未读
- 修复管理中心帖子接口使用错误字段导致帖子列表为空
- 修复编辑用户时未加载当前铭牌和认证标志
- 修复 `[hide]` 内的图片、链接、Markdown 等 BBCode 无法继续渲染
- 修复 `[url]`、`[markdown]` 等 BBCode 前后被额外插入空白的问题

### 服务端

- 媒体 BBCode 自动把本站附件相对地址规范化为当前站点绝对地址
- 管理帖子接口映射到现有 `is_top`、`is_closed`、`reply_count` 字段
- 新增 `upgrade_version_1_1_7.php` 版本登记脚本

---

## v1.1.6

### 新功能

- 新增全局音乐播放器、上一曲/下一曲和播放列表
- 工具页新增音乐播放器入口，支持从帖子音乐卡片打开
- 音乐使用全局播放器，切换页面或进入后台后继续播放

### Bug 修复

- 修复管理员用户组发布帖子时不显示附件上传入口
- 修复音乐卡片封面和原始文件名无法正确显示
- 修复私信对话页面因用户 ID 类型异常而无法渲染
- 修复首页私信图标不更新未读数量角标
- 修复客户端分享链接未指向帖子分享页

### 服务端

- 上传媒体恢复包含当前服务器域名的绝对 URL
- 分享接口返回 `share.php?id=帖子ID` 地址
- 新增 `upgrade_version_1_1_6.php` 版本登记脚本

---

## v1.1.5

### 新功能

- **账号与安全**：新增独立页面，支持修改密码、查看/撤销登录设备、退出登录
- **深度链接**：支持 `hyjzbbs://` 协议，可从浏览器直接跳转 App 内帖子（格式 `hyjzbbs://thread/ID`）
- **下载管理**：独立下载页面，支持新建下载、暂停/继续/取消/打开/重试/打开文件夹，显示文件保存路径
- **下载 Toast**：开始下载时 Toast 右侧新增「查看下载」按钮，一键跳转下载管理
- **DV 码**：每个帖子生成唯一 DV 码，可作为帖子标识符分享
- **工具页**：底部导航「发现」改为「工具」，整合下载管理、热门话题、热帖榜等功能入口
- **设备名识别**：登录设备列表显示真实设备型号（如 Xiaomi 14、DESKTOP-ABC），需重新登录生效
- **B 站风格视频播放器**：单击显隐面板、双击 ±10s 快进快退、水平拖动 seek、长按 2 倍速、进度条拖拽预览
- **推荐算法优化**：加权随机抽取（指数分布法）、用户兴趣画像、新鲜度随机因子、客户端去重

### 管理中心

- **用户权限编辑**：支持切换用户组、单独权限覆盖（发帖/评论/删除等 8 项权限，允许/禁止/继承）
- **帖子管理**：新增独立页面，支持搜索、按状态筛选（公开/待审核/私密/锁定）、置顶、锁定、删除

### 变更

- 底部导航「发现」改为「工具」，图标更换
- 版本号与构建号硬编码在 `AppConfig` 中，移除 `package_info_plus` 运行时依赖
- 检查更新自动适配平台（`download.php?os=xxx`），使用内置下载器下载，完成后自动打开安装包

### Bug 修复

- 修复帖子详情页内容区域渲染为灰色大块的问题（`Column` 添加 `mainAxisSize.min`）
- 修复创建帖子时 DV 码唯一索引冲突导致发布失败
- 修复 `DvCode.php` 未被 `index.php` 引用导致类找不到

### 服务端

- `AuthController` 新增 `changePassword`、`sessions`、`revokeSession` 接口
- `Auth::createToken` 优先使用 `X-Device-Name` 存储设备名称
- `AdminController` 新增 `groupList`、`threads`、`threadDelete`、`threadToggleSticky`、`threadToggleLock` 接口
- `AdminController::updateUser` 支持修改 `group_id` 和 `permissions`
- `ThreadCreateController` 发布失败时返回具体错误信息
- 新增 `upgrade_version_1_1_5.php` 升级脚本

---

## v1.1.4

### 新功能

- **铭牌系统**：管理员可为用户设置 2-5 字自定义颜色铭牌
- **认证标志**：已认证（绿 V）、官方（蓝 V）、知名人物（金 V）
- **DV 号系统**：帖子唯一标识，base-54 编码，去除易混淆字符
- **帖子链接卡片**：`[thread=DV码]` BBCode 标签，渲染为横向卡片
- **内置多线程下载器**：4 线程分片下载、断点续传、进度回调
- **永久下载页**：`download.php?os=android` 直接跳转下载链接
- **注册页昵称**：注册时可选填昵称

### 变更

- 用户名不再允许中文，仅限字母、数字、下划线
- 移除 `permission_handler` 依赖，Windows 构建不再需要额外权限
- Windows 构建强制 `CMAKE_INSTALL_PREFIX` 指向构建目录

---

## v1.1.3

### Bug 修复

- 修复深色模式下卡片/文字颜色不适配

---

## v1.1.2

### Bug 修复

- 修复附件下载无反应及 `[url]` 标签前后多余换行

---

## v1.1.1

### 新功能

- 附件上传功能（管理员专用）
- 评论输入框添加表情包功能

---

## v1.1.0

### 新功能

- 签到系统
- 用户主页
- 搜索功能
- 浏览历史
- 私信系统
- 通知系统
- 创作者中心
- 管理中心（用户管理、内容审核、系统设置）
- 发现页
- 动态页

---

## v1.0.0

### 初始版本

- 首页瀑布流
- 帖子详情与 BBCode 渲染
- 注册/登录
- 个人资料编辑
- 板块列表
- 帖子发布（图文/文章模式）
- 点赞/收藏/分享/举报
- 评论系统
- 深色模式
- HTTPS 支持
